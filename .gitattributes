# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must always use LF, even on Windows checkouts, or they
# break on Linux (bad interpreter, CRLF issues)
*.sh text eol=lf
