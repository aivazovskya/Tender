// Single source of truth for the footer copyright line, so every route
// (catalog, login, tender detail) shows identical text instead of each
// page carrying its own hand-written variant. Mirrors translations.ts'
// `footer.rights` (RU) for pages that aren't wired into the i18n context.
export const FOOTER_RIGHTS_TEXT = '© 2026 TenderAI Kazakhstan. Все права защищены.';
