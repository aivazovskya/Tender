/**
 * Global risk and contract execution constants.
 */

/**
 * Default daily contract penalty rate (0.1% per day = 0.001).
 * Standard default based on public procurement contracts (analogous to 44-FZ / Goszakup RK).
 */
export const DEFAULT_PENALTY_RATE_PER_DAY = 0.001;
